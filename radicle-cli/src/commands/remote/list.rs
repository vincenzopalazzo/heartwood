use radicle::storage::git::Repository;
use radicle::Profile;

/// core command to run the `rad remote list` or just `rad remote` subcommand.
pub fn run(repository: &Repository, profile: &Profile) -> anyhow::Result<()> {
    Ok(())
}
