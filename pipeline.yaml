---
image: native # or docker:<image name>:<image_tag>
actions:
    -
      on: [ "patch", "push" ]
      verbose: true # see all the logs generated by the action
      run: | 
        cargo build  
        cargo clippy --all --tests 
        cargo fmt --all --check 
        cargo test --all
